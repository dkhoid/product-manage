mixin form-change-multi(path)
    form(action=path
        method="POST"
        id="form-change-multi-status"

    )
        .d-flex.align-items-start
            .form-group
                select.form-control(name="type" id="control" title="Chọn trạng thái cần thay đổi")
                    option(value="Active") Hoạt động
                    option(value="Inactive") Ngừng hoạt động
                    option(value="Delete-all") Xóa đồng thời
            .form-group
                input(type='text' name='ids' value='' class='form-control d-none')

            button(type='submit' class = "btn btn-primary") Áp dụng
