mixin alert-success(time)
    - if(messages.success)
        .message.info
            div(class="alert alert-success"
                show-alert
                data-timeout=time
            ) #{messages.success} <span close-alert> &times; </span>